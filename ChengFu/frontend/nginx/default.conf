# ============================================================
# 承富 AI · nginx 反向代理
# ============================================================
# 職責:
#   / 與 /static/*  → 承富 launcher(靜態檔)
#   /chat 與其他    → 代理到 LibreChat(port 3080)
#   將客製 CSS / JS 注入 LibreChat 回應
# ============================================================

# 日誌
log_format chengfu '$remote_addr - $remote_user [$time_local] '
                   '"$request" $status $body_bytes_sent '
                   '"$http_referer" "$http_user_agent"';

server {
    listen 80;
    server_name localhost 承富-ai.local _;
    server_tokens off;

    access_log /var/log/nginx/chengfu.access.log chengfu;
    error_log  /var/log/nginx/chengfu.error.log warn;

    client_max_body_size 60M;  # 與 LibreChat fileConfig 匹配(50MB + buffer)

    # ==========================================================
    # 1. 承富 Launcher 首頁
    # ==========================================================
    # 根路徑只回傳 launcher,不代理到 LibreChat
    location = / {
        root /var/www/launcher;
        try_files /index.html =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # ==========================================================
    # 1.5 攔截 LibreChat 的 Workbox Service Worker(路線 A 必要)
    # 使用者瀏覽器若註冊過 LibreChat SW,回傳「自我銷毀」腳本
    # ==========================================================
    location = /sw.js {
        root /var/www/launcher;
        try_files /kill-sw.js =404;
        add_header Content-Type application/javascript;
        add_header Service-Worker-Allowed "/";
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
    location ~ ^/workbox-.*\.js$ {
        root /var/www/launcher;
        try_files /kill-sw.js =404;
        add_header Content-Type application/javascript;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # 承富自己的 SW · 限定 scope 在 / · 不碰 LibreChat 路徑
    # 關鍵:SW 必須 no-cache · 否則 SW 更新失效(瀏覽器拿不到新版 sw.js)
    location = /static/sw.js {
        root /var/www;
        try_files /launcher/sw.js =404;
        add_header Content-Type application/javascript;
        add_header Service-Worker-Allowed "/";
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    # Launcher 的靜態檔(CSS / JS)· dev 階段不 cache
    location /static/ {
        alias /var/www/launcher/;
        try_files $uri =404;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
    }

    # 承富客製 CSS / JS(注入到 LibreChat 頁面)
    location /chengfu-custom/ {
        alias /var/www/custom/;
        try_files $uri =404;
        expires 1h;
    }

    # ==========================================================
    # 1.8 路線 A · 強制重導 LibreChat SPA 路徑到承富 Launcher
    # 302 於瀏覽器 request LibreChat UI 時生效 · 使用者無法看到 LibreChat 介面
    # ==========================================================
    location = /c/new { return 302 /; }
    location ~ ^/c/(.+)$ { return 302 /?convo=$1; }

    # 承富客製 CSS / JS · 加強 cache-buster
    location ~ ^/chengfu-custom/(.+)$ {
        alias /var/www/custom/$1;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        add_header Pragma "no-cache";
        add_header Expires "0";
    }

    # ==========================================================
    # 2. 導向規則
    # ==========================================================
    # /login /register 導回 LibreChat · 注入承富客製 JS(路線 A · 登入後彈回 /)
    location = /login {
        proxy_pass http://librechat:3080/login;
        include /etc/nginx/chengfu-proxy.conf;
        sub_filter '</head>' '<link rel="stylesheet" href="/chengfu-custom/librechat-custom.css"><script src="/chengfu-custom/librechat-relabel.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }
    location = /register {
        proxy_pass http://librechat:3080/register;
        include /etc/nginx/chengfu-proxy.conf;
        sub_filter '</head>' '<link rel="stylesheet" href="/chengfu-custom/librechat-custom.css"><script src="/chengfu-custom/librechat-relabel.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }
    location = /forgot-password {
        proxy_pass http://librechat:3080/forgot-password;
        include /etc/nginx/chengfu-proxy.conf;
    }

    # LibreChat 相對路徑的對話頁:/chat -> /c/new, /chat/c/xxx -> /c/xxx
    location /chat {
        rewrite ^/chat$ /c/new permanent;
        rewrite ^/chat/(.*)$ /$1 break;
        proxy_pass http://librechat:3080;
        include /etc/nginx/chengfu-proxy.conf;

        # 注入承富客製 CSS + JS 到 HTML 回應
        sub_filter '</head>' '<link rel="stylesheet" href="/chengfu-custom/librechat-custom.css"><script src="/chengfu-custom/librechat-relabel.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }

    # ==========================================================
    # 3. LibreChat API(直接代理)
    # ==========================================================
    location /api/ {
        proxy_pass http://librechat:3080/api/;
        include /etc/nginx/chengfu-proxy.conf;

        # SSE 需要長連線
        proxy_buffering off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    # ==========================================================
    # 3.1 會計 API(承富內建 FastAPI)
    # ==========================================================
    location /api-accounting/ {
        rewrite ^/api-accounting/(.*)$ /$1 break;
        proxy_pass http://accounting:8000;
        include /etc/nginx/chengfu-proxy.conf;
    }

    # ==========================================================
    # 4. LibreChat 其他資源(images / assets / fonts / __CONFIG)
    # ==========================================================
    location ~ ^/(images|assets|fonts|_next|favicon|manifest|robots) {
        proxy_pass http://librechat:3080;
        include /etc/nginx/chengfu-proxy.conf;
        expires 1d;
        add_header Cache-Control "public, max-age=86400";
    }

    # ==========================================================
    # 5. 其他所有 → LibreChat(含 / 以外的 root 路徑)
    # 注入客製 CSS/JS 到 HTML
    # ==========================================================
    location / {
        proxy_pass http://librechat:3080;
        include /etc/nginx/chengfu-proxy.conf;

        sub_filter '</head>' '<link rel="stylesheet" href="/chengfu-custom/librechat-custom.css"><script src="/chengfu-custom/librechat-relabel.js"></script></head>';
        sub_filter_once on;
        sub_filter_types text/html;
    }

    # ==========================================================
    # 健康檢查
    # ==========================================================
    location = /healthz {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
